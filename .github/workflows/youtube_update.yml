name: Check for ReVanced Updates (YouTube)

on:
  schedule:
    - cron: "0 1 * * *" # Run daily at 1 AM (separate from Photos)
  workflow_dispatch:

jobs:
  check-youtube:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: "zulu"
          java-version: "17"

      - name: Install dependencies
        run: |
          pip install -r scripts/requirements.txt

      - name: Install apkeep (python script)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python scripts/install_apkeep.py
          chmod +x apkeep
          sudo mv apkeep /usr/local/bin/
          apkeep --version

      - name: Run update script (Check versions)
        id: check
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SKIP_CLEANUP: "true"
        run: python scripts/check_updates.py

      - name: Download YouTube APK
        if: steps.check.outputs.youtube_version != ''
        run: |
          VERSION="${{ steps.check.outputs.youtube_version }}"
          echo "Expected version: $VERSION"
          if [ "$VERSION" = "latest" ]; then
            echo "Downloading latest version..."
            apkeep -a com.google.android.youtube .
          else
            echo "Downloading version $VERSION..."
            apkeep -a com.google.android.youtube@$VERSION .
          fi

      - name: Patch YouTube
        if: steps.check.outputs.youtube_version != ''
        run: |
          echo "Current directory contents:"
          ls -R

          # Find the downloaded APK (broad matching)
          APK_FILE=$(find . -maxdepth 1 -name "com.google.android.youtube*" | head -n 1)

          if [ -z "$APK_FILE" ]; then
             echo "APK not found! Search failed."
             exit 1
          fi

          echo "Found APK file/bundle: $APK_FILE"

          # Handle XAPK/APKS (Split APKs) which apkeep downloads from Play Store
          if [[ "$APK_FILE" == *.xapk ]] || [[ "$APK_FILE" == *.apks ]]; then
            echo "Detected Split APK bundle ($APK_FILE). Extracting and Merging..."
            unzip -o "$APK_FILE" -d extracted_apk
            
            # Download APKEditor to merge splits
            echo "Downloading APKEditor to merge split APKs..."
            curl -L -o APKEditor.jar https://github.com/REAndroid/APKEditor/releases/download/V1.4.3/APKEditor-1.4.3.jar
            
            # Merge the split APKs into a single APK
            java -jar APKEditor.jar m -i extracted_apk -o merged.apk
            
            if [ ! -f "merged.apk" ]; then
              echo "Error: Failed to merge APKs."
              exit 1
            fi
            
            echo "Successfully merged APKs into merged.apk"
            APK_FILE="merged.apk"
          fi

          CLI_JAR=$(find . -name "revanced-cli-*.jar" | head -n 1)
          PATCHES_RVP=$(find . -name "patches-*.rvp" | head -n 1)

          echo "Patching $APK_FILE..."

          # Use --exclusive with -e to enable only specific patches
          # Do NOT use -i, as that is for --install
          java -jar "$CLI_JAR" patch \
            --exclusive \
            --keystore "google-photos-revanced.keystore" \
            -p "$PATCHES_RVP" \
            -e "Bypass URL redirects" \
            -e "Bypass image region restrictions" \
            -e "Change form factor" \
            -e "Check watch history domain name resolution" \
            -e "Copy video URL" \
            -e "Custom player overlay opacity" \
            -e "Disable auto captions" \
            -e "Disable double tap actions" \
            -e "Disable fullscreen ambient mode" \
            -e "Disable haptic feedback" \
            -e "Disable player popup panels" \
            -e "Disable resuming Shorts on startup" \
            -e "Disable rolling number animations" \
            -e "Disable sign in to TV popup" \
            -e "Disable video codecs" \
            -e "Downloads" \
            -e "Exit fullscreen mode" \
            -e "Force original audio" \
            -e "GmsCore support" \
            -e "Hide Shorts components" \
            -e "Hide ads" \
            -e "Hide end screen cards" \
            -e "Hide end screen suggested video" \
            -e "Hide info cards" \
            -e "Hide layout components" \
            -e "Hide player flyout menu items" \
            -e "Hide player overlay buttons" \
            -e "Hide related video overlay" \
            -e "Hide timestamp" \
            -e "Hide video action buttons" \
            -e "Loop video" \
            -e "Miniplayer" \
            -e "Navigation buttons" \
            -e "Open Shorts in regular player" \
            -e "Open links externally" \
            -e "Open videos fullscreen" \
            -e "Playback speed" \
            -e "Remove background playback restrictions" \
            -e "Remove viewer discretion dialog" \
            -e "Return YouTube Dislike" \
            -e "Sanitize sharing links" \
            -e "Seekbar" \
            -e "Shorts autoplay" \
            -e "SponsorBlock" \
            -e "Spoof app version" \
            -e "Spoof device dimensions" \
            -e "Spoof video streams" \
            -e "Swipe controls" \
            -e "Theme" \
            -e "Video ads" \
            -e "Video quality" \
            -e "Wide search bar" \
            -o youtube-revanced.apk \
            "$APK_FILE"

      - name: Upload YouTube ReVanced
        if: steps.check.outputs.youtube_version != ''
        uses: actions/upload-artifact@v4
        with:
          name: youtube-revanced
          path: youtube-revanced.apk
          retention-days: 90

      - name: Create Release
        if: steps.check.outputs.youtube_version != ''
        uses: softprops/action-gh-release@v1
        with:
          tag_name: youtube-revanced-v${{ steps.check.outputs.youtube_version }}
          name: YouTube ReVanced v${{ steps.check.outputs.youtube_version }}
          body: |
            Automated build of YouTube ReVanced.

            **Version:** ${{ steps.check.outputs.youtube_version }}

            *Signed by Google (verified).*
          files: youtube-revanced.apk
          draft: false
          prerelease: false
